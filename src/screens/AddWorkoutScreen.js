import React from 'react';
import WorkoutForm from '../components/WorkoutForm';

const AddWorkoutScreen = ({ navigation }) => {
    return <WorkoutForm navigation={navigation} />;
};

export default AddWorkoutScreen;
